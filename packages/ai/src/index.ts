/**
 * @spherewiki/ai — embeddings, RAG retrieval, and the on-save agent.
 * Runs with the org's BYO Claude key for generation; embeddings run on-device.
 *
 * M0 stub — real modules land in M4 (see docs/ROADMAP.md).
 */

/** Default on-device embedding model (decision AD-2). */
export const EMBEDDING_MODEL = "multilingual-e5-small" as const

#!/usr/bin/env node
// Rebuild a workspace's derived stores (DuckDB / pgvector) from Markdown.
// Stub for M0 — the real idempotent rebuild lands with the index in M2/M4.
// See docs/ROADMAP.md and the "idempotent re-indexing" invariant in CLAUDE.md.
console.log("[reindex] not implemented yet — derived-store rebuild lands in M2/M4.")

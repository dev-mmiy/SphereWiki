/**
 * @spherewiki/desktop — the local-first client: Tauri 2 (Rust core) + React + CodeMirror 6,
 * with a local DuckDB index and native `yrs` for CRDT sync.
 *
 * M0 stub — the real app lands in M2 (see docs/ROADMAP.md).
 */

export const APP_NAME = "SphereWiki" as const
